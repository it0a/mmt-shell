#!/bin/bash
do_init() {
    if [ "$1" = "dump" ]
    then
        BCMT_INITOK="0"
        BCMT_OPTION="dump"
    elif [ "$1" = "restore" ]
    then
        BCMT_INITOK="0"
        BCMT_OPTION="restore"
    else
        BCMT_INITOK="1"
    fi
}
