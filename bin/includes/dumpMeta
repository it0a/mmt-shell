#!/bin/bash
dump_table() {
    unk_status "Dumping $1..."
    mysqldump -h $BCMT_HOST -P $BCMT_PORT -u $BCMT_USER -p$DBPASS --extended-insert=FALSE --skip-comments $BCMT_DBNAME $1 > $SCRIPTPATH/dump/$1.sql
    unk_ok_status
}
